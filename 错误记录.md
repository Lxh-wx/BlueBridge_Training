# 错误记录

按日期 + 题号归档，考前回顾用。

## 2026-05-26/27 数据结构专题

### P3367 并查集

- **逻辑**：合并时改了节点本身而不是根，导致部分节点被落下（如 4←3←2→1，改 3 的父节点后 4 还指向旧根）。应 `vis[find(y)] = find(x)` 改根。
- **逻辑**：查询时直接比 `vis[x] == vis[y]`（父节点），应比 `find(x) == find(y)`（根）。
- **逻辑**：`if(x[i] <= y[i])` 判断合并方向，跟集合结构无关，毫无意义。

### P1551 亲戚

- **语法**：`cout << 'Yes'` 用了单引号，单引号是多字符字面量，输出整数而非字符串。应 `cout << "Yes"`。

### P3374 树状数组1

- **逻辑**：`query` 里 `ans += a[x]` 读取原数组而非树数组 `tree[x]`，树状数组存的值都在 tree 里。
- **语法**：`query` 返回 `int`，但前缀和可能超 2^31，应返回 `ll`；内部 `ans` 也应为 `ll`。
- **语法**：数组开 50005，n 最大 5e5，越界。

### P3368 树状数组2

- **逻辑**：不理解差分。原值建树 `update(i, a[i])` 后不能用差分技巧（`update(y+1, -k)`），必须全套差分（建树用 `a[i]-a[i-1]`）才行。
- **逻辑**：`update(y+1, -k)` 少算了一位，差分边界纠正应从 y+1 开始。
- **逻辑**：查询多余：`query(x) - query(x-1)`，差分下 `query(x)` 本身就是单点值。

### P3865 ST表

- **语法**：数组声明缺分号、内层循环变量写错（j 写成 i）、数组名打错（s 写成 st）。
- **语法**：`1<<j-1` 优先级，减号高于左移，实际是 `1<<(j-1)`，建议加括号。
- **语法**：`endl` 刷新缓冲区，2e6 次查询每次刷新 → 超时。用 `'\n'` 代替。
- **逻辑**：`log2()` 浮点函数，2e6 次调用速度慢。预处理 `lg[i] = lg[i/2] + 1`，O(1) 查表。
- **逻辑**：输入循环 `i=0; i<=n` 多读一个数。

## 2026-05-27 数论板子

### P1226 快速幂

无大错。旧版代码 `a` 在循环中被改但输出在循环前避免了问题。变量命名 `ras`→`res`。

### P3383 欧拉筛

- **逻辑**：标记和 break 顺序反了——先 `if(i%e==0) break;` 再 `notp[e*i]=1;`，导致 `i % prime == 0` 时该标记的合数被跳过。正确顺序：**先标记，再 break**。
- **语法**：`i * e` 可能 int 溢出，需 `(ll)i * e`。

### P1029 GCD & LCM

- **语法**：`int S = x * y`，x、y 到 1e5 时 S 到 1e10，int 溢出。必须 `ll S = (ll)x * y`。
- **语法**：`i*i <= S` 中 `i` 是 int，i=5e4 时 i*i=2.5e9 溢出。`i` 改为 `ll`。
- **逻辑**：P=Q（完全平方数）时 `ans -= 1` 防重复计数。
- **逻辑**：gcd 函数 `a%b==0` 当 b=0 时除零崩，用标准 `return b==0 ? a : gcd(b, a%b)`。

## 2026-05-27/28 图论最短路

### P3371 Dijkstra 弱化版

- **语法**：`struct edge{}` 右花括号后缺分号。
- **语法**：`vector<edge> G(n+1)` 是单个 vector，邻接表应 `vector<edge> G[N]`（N 个 vector）。
- **语法**：`G[u].push_back(v, w)` 只接受一个参数，应 `push_back({v, w})`。
- **语法**：`memset(dis, INf, sizeof(d))` — `INf` 大小写错，`sizeof(d)` 变量名写错。
- **语法**：`auto [d, u] = pq.top()` 是 C++17 结构化绑定，蓝桥杯 C++11 不支持，应 `.first` `.second`。
- **语法**：`pq.push(dis[v], v)` 缺大括号，应 `pq.push({dis[v], v})`。
- **语法**：`int v=e.to; w=e.w;` 缺第二个 `int`。
- **逻辑**：`push` 写在 `if` 外面，每条边都入堆导致堆爆炸，应只在 `if(dis[v] > dis[u] + w)` 内 push。
- **逻辑**：不可达点直接输出 `dis[i]`（0x3f3f...），题目要求输出 `2147483647`，需判断后替换。

### P1629 邮递员送信

- **语法**：`qair` 拼写错误 → `pair`；`{0.s}` 句号 → `{0, s}`。
- **逻辑**：第二次 Dijkstra 调用还是 `G1, dis1`，返程没算。应改为 `G2, dis2`。
- **逻辑**：两次 Dijkstra 之间需要 `memset(vis, 0, sizeof(vis))` 重置标记数组。
