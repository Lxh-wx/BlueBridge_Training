# 国赛数据结构与算法匹配手册

> 目标：看到题目 → 识别出数据结构 → 知道用哪些算法 → 回忆起板子细节
> 基于 2016-2025 年 C/C++ B 组 86 道真题调研

---

## 总览：数据结构 → 算法 → 问题场景

```
数据结构           匹配算法                  国赛频次    典型年份
────────────────────────────────────────────────────────────
数组/前缀和/差分    前缀和、差分、双指针        ★★★★★    每年都有
树（一般树）        LCA、树上差分、树形DP      ★★★★     22/23连考
图（邻接表）        Dijkstra/Kruskal/拓扑      ★★★★     23图论大年
并查集              Kruskal、连通性、判环      ★★★      17/22/23
字符串             KMP、DP子序列              ★★★      20/24
堆/优先队列         Dijkstra、贪心、哈夫曼     ★★★      20/22/23
树状数组            单点修改+区间查询           ★★       19/21
ST表               RMQ（静态区间最值）         ★★       22机房
线段树              区间修改+区间查询           ★        19/21
栈/单调栈           单调性维护                 ★        隐含
```

---

## 一、数组 + 前缀和 + 差分（最基础、最常用）

### 数据结构本质

| 元素 | 名称 | 含义 |
|------|------|------|
| `a[i]` | 原数组 | 题目给的数据 |
| `pre[i]` | 前缀和数组 | `pre[i] = pre[i-1] + a[i]`，前 i 个元素的和 |
| `diff[i]` | 差分数组 | `diff[i] = a[i] - a[i-1]`，原数组的"变化率" |

### 匹配算法

| 算法 | 解决的问题 | 国赛真题 |
|------|-----------|---------|
| **一维前缀和** | 快速求任意区间 `[l, r]` 的和 → `pre[r] - pre[l-1]` | 2021 123 题、2023 合并数列 |
| **二维前缀和** | 快速求子矩阵和 → `s[x2][y2] - s[x1-1][y2] - s[x2][y1-1] + s[x1-1][y1-1]` | 2018 搭积木 |
| **一维差分** | 多次区间加减 `[l, r]` 后求最终数组 → `diff[l] += x, diff[r+1] -= x` + 前缀和还原 | 2023 抓娃娃 |
| **双指针** | 有序数组找一对/合并两段 | 2023 合并数列 |

### 关键性质

- 前缀和还原区间和是 O(1) 的 —— 这就是它的全部价值
- 差分还原原数组：`a[i] = diff[1] + diff[2] + ... + diff[i]`（前缀和）
- 遇到"多次区间加减"→ 差分；"多次区间求和"→ 前缀和
- **二维前缀和最容易写错的坑**：存储方向与查询方向一致。按列存前缀和，查询时用行号减；按行存前缀和，查询时用列号减

### 代码骨架

```cpp
// 一维前缀和（求 [l, r] 和，O(1)）
pre[i] = pre[i-1] + a[i];
ans = pre[r] - pre[l-1];

// 一维差分（区间 [l, r] 加 x，最终还原）
diff[l] += x;
diff[r+1] -= x;
// 还原：for i=1..n: a[i] = a[i-1] + diff[i];
```

---

## 二、树（Tree）— 无向无环连通图

### 数据结构本质

| 名称 | 含义 |
|------|------|
| **根 (root)** | 任意选一个节点作为起点 |
| **父节点 (parent)** | 从根往下走，直接相连的上层节点 |
| **深度 (depth)** | 节点到根经过的边数，`depth[root] = 0` |
| **祖先/子孙** | 根到节点路径上的所有节点都是祖先；反之是子孙 |
| **LCA (最近公共祖先)** | 两个节点往根走，第一个相遇的节点 |
| **子树** | 以某节点为根的整棵下级树 |
| **边权/点权** | 边上或节点上的数值 |

### 为什么树要用 LCA？

**核心原因：树上两点之间的路径唯一。** 图里两点之间可能有多条路，但树里只有一条——就是从 u 往上到 LCA，再从 LCA 往下到 v。

```
     u ─→ ─→ LCA ─→ ─→ v
     往上走    往下走
```

这条路径的长度 = `depth[u] + depth[v] - 2 * depth[lca]`

### 匹配算法

| 算法 | 解决的问题 | 国赛真题 |
|------|-----------|---------|
| **LCA 倍增法** | 树上两点路径长度、路径上的特殊点 | 2022 机房、2023 景区导游 |
| **树上差分** | 多次给树上某条路径所有边/点加 x，最后问每条边/点的值 | 2023 砍树 |
| **树形 DP** | 子树最优解（选了父节点子节点不能选等依赖关系） | 2021 最小权值 |
| **树的直径** | 树中最远两点距离（两次 BFS 或树形 DP） | 图论补漏 |

### 关键性质

1. **n 个节点的树有 n-1 条边**，多一条就有环
2. 树上两点路径唯一 → 这是所有树上算法的基础
3. **LCA 倍增板子要点**（容易出错的地方）：
   - `up[i][j]`：节点 i 往上跳 `2^j` 步到达的节点，`up[i][0]` = 父节点
   - 第二维大小：`log2(n)` ≈ 20（n ≤ 5×10⁵），循环用 `< 20` 不用 `<= 20`
   - `up[root][0] = root` 作为哨兵，防止跳过头
   - `1 << j` 运算优先级低于 `>` `<`，必须写成 `(1 << j)`
   - 对齐深度后先判 `if (u == v) return u`
4. **树上差分**：路径 `(u, v)` 加 x →
   `diff[u] += x, diff[v] += x, diff[lca] -= 2x`（边差分）
   `diff[u] += x, diff[v] += x, diff[lca] -= x, diff[parent[lca]] -= x`（点差分）
   最后做一次 DFS 自底向上求和还原

### 树上差分的代码骨架

```cpp
// 树上边差分：给路径 (u, v) 上所有边 +1
diff[u]++; diff[v]++;
diff[lca] -= 2;

// DFS 自底向上求和（还原每条边的最终值）
void dfs_sum(int u, int fa) {
    for (int v : G[u]) {
        if (v == fa) continue;
        dfs_sum(v, u);
        diff[u] += diff[v];
    }
    // 此时 diff[u] 就是边 (u, fa) 的最终值
}
```

---

## 三、图（Graph）— 节点 + 边

### 数据结构本质

| 名称 | 含义 |
|------|------|
| **邻接表 `vector<pair<int,int>> G[N]`** | 存储每个节点连出去的边（目标节点，边权） |
| **邻接矩阵 `int g[N][N]`** | n×n 矩阵存边权，适合 n ≤ 500 的稠密图 |
| **入度/出度** | 指向该节点的边数 / 从该节点出去的边数 |
| **连通分量** | 图中互相可达的最大子图 |
| **DAG（有向无环图）** | 没有环的有向图，可以拓扑排序 |

### 为什么不同问题用不同算法？

图算法选择的核心依据：

```
图有没有负权边？
  ├── 没有 → Dijkstra（堆优化 O(mlogm)）
  │      └── 稠密图(m≈n²) → 朴素 O(n²) 也不差
  └── 有   → SPFA 或 Bellman-Ford（B 组极少考）

需不需要所有点之间的最短路？
  ├── 需要 → Floyd（O(n³)，n ≤ 500）
  │      └── 离线加点动态更新 → Floyd 天然支持
  └── 不需要 → 见上

是不是 DAG（有向无环）？
  ├── 是 → 拓扑排序 + DP
  └── 不是 → 见上

问的是最小生成树？
  └── Kruskal（O(mlogm)，并查集）
```

### 匹配算法

| 算法 | 解决的问题 | 国赛真题 |
|------|-----------|---------|
| **Dijkstra（堆优化）** | 单源最短路（边权非负） | 2022 出差 |
| **Floyd** | 全源最短路 / 动态加点最短路径 | 2024 未直接考但板子必备 |
| **Kruskal + 并查集** | 最小生成树（n 个点连通的 n-1 条边最小代价） | B 组未直接考，但 MST 思想重要 |
| **拓扑排序** | DAG 上的最长路 / 计数路径 / 任务调度 | 未直接考但板子必备 |
| **Tarjan（割边/割点）** | 图中删哪条边/哪个点会让图不连通 | 2023 删边问题 |

### 关键性质

1. **邻接表 `vector<pair<int,int>> G[N]`** 是国赛最常用的图存储方式
2. **Dijkstra 的堆写法易错点**：
   - `push` 必须写在 `if (dis[v] > dis[u] + w)` 里面，写外面会堆爆炸
   - 优先队列默认大根堆，小根堆用 `greater<pair<int,int>>`
   - C++11 不支持结构化绑定，用 `.first` `.second`
3. **Floyd 就 5 行**：`for(k) for(i) for(j) g[i][j] = min(g[i][j], g[i][k] + g[k][j])`
   - k 必须在最外层！这是最容易被改错的
   - Floyd 天然支持动态加点：逐 k 解锁中间节点
4. **拓扑排序核心**：入度 = 0 的点入队 → 逐一出队，邻居入度 -1，新的入度 0 点入队
5. **正反图技巧**：求"每个点去源点 + 源点去每个点"的最短路 → 正向图 + 反向图各跑一次 Dijkstra

### 代码骨架

```cpp
// Dijkstra 堆优化
vector<pair<int,int>> G[N];  // G[u] = {v, w}
priority_queue<pair<int,int>, vector<pair<int,int>>, greater<>> pq;
// pq = {dis, node}
memset(dis, 0x3f, sizeof(dis));
dis[s] = 0; pq.push({0, s});
while (!pq.empty()) {
    auto [d, u] = pq.top(); pq.pop();  // C++17 结构化绑定
    // C++11 用: int d = pq.top().first, u = pq.top().second;
    if (d != dis[u]) continue;
    for (auto [v, w] : G[u]) {
        if (dis[v] > dis[u] + w) {
            dis[v] = dis[u] + w;
            pq.push({dis[v], v});  // 必须在 if 里面！
        }
    }
}

// Floyd（全源最短路）
for (int k = 1; k <= n; k++)           // k 必须在最外层！
    for (int i = 1; i <= n; i++)
        for (int j = 1; j <= n; j++)
            g[i][j] = min(g[i][j], g[i][k] + g[k][j]);
```

---

## 四、并查集（DSU / Union-Find）

### 数据结构本质

| 名称 | 含义 |
|------|------|
| `fa[i]` | 节点 i 的父节点（初始 `fa[i] = i`） |
| `find(x)` | 找 x 所在集合的根（代表元），带路径压缩 |
| `merge(x, y)` | 合并 x 和 y 所在的集合 |

### 匹配算法

| 算法 | 如何用并查集 | 国赛真题 |
|------|-------------|---------|
| **Kruskal（MST）** | 边按权值排序，逐个判断两端点是否同集合，不同则合并（即选这条边） | MST 通用 |
| **判环** | 遍历边时发现两端已同集合 → 加这条边会成环 | 2017 发现环 |
| **连通性查询** | 两个点同集合 = 连通 | 通用 |

### 关键性质

1. **路径压缩**：`find(x)` 返回根的同时把沿途节点的父节点都改为根，摊还 O(α(n)) ≈ 常数
2. **合并时永远改根不改节点**：`fa[find(x)] = find(y)`，不是 `fa[x] = y`
3. **判连通**：比 `find(x) == find(y)`，不要比 `fa[x] == fa[y]`
4. 并查集是最简单的数据结构之一，但也是 Kruskal 和判环的前置条件，**必须闭眼写对**

### 代码骨架

```cpp
int fa[N];
int find(int x) {
    return fa[x] == x ? x : fa[x] = find(fa[x]);
}
void merge(int x, int y) {
    fa[find(x)] = find(y);
}
// 初始化：for (int i = 1; i <= n; i++) fa[i] = i;
```

---

## 五、树状数组（Fenwick Tree / BIT）

### 数据结构本质

| 名称 | 含义 |
|------|------|
| `tree[i]` | 管辖 `a[i - lowbit(i) + 1]` 到 `a[i]` 这一段的和 |
| `lowbit(x)` | `x & -x`，x 二进制最低位的 1 代表的值 |
| `update(i, x)` | 给位置 i 加 x，O(log n) |
| `query(i)` | 查前缀和 [1, i]，O(log n) |

### 解决什么问题？

**频繁的单点修改 + 频繁的前缀/区间查询**，两者都 O(log n)。

暴力数组：修改 O(1) 但查询 O(n)；前缀和数组：查询 O(1) 但修改 O(n)。树状数组折中。

### 匹配算法

| 变种 | 解决的问题 | 国赛真题 |
|------|-----------|---------|
| **树状数组1**：tree 存原值 | 单点修改 + 区间求和 `query(r) - query(l-1)` | 2019 第八大奇迹 |
| **树状数组2**：tree 存差分 | 区间修改（差分 `update(l, x), update(r+1, -x)`）+ 单点查询 `query(x)` | 通用 |

### 关键性质

1. **`tree[i]` 不是原数组值！** 用 `tree[i]` 当原数组读是高频错误
2. 差分版（区间修改+单点查询）：建树用 `update(i, a[i] - a[i-1])`，不能用原值建树
3. 树状数组从下标 1 开始，0 会死循环（lowbit(0) = 0）
4. 答案可能超 `int`，`query` 返回 `ll`

### 代码骨架

```cpp
int tree[N], n;

int lowbit(int x) { return x & -x; }

void update(int i, int x) {  // 给位置 i 加 x
    while (i <= n) {
        tree[i] += x;
        i += lowbit(i);
    }
}

ll query(int i) {  // 查前缀和 [1, i]
    ll ans = 0;
    while (i > 0) {
        ans += tree[i];
        i -= lowbit(i);
    }
    return ans;
}

// 区间求和：query(r) - query(l-1)
// 区间修改：update(l, x); update(r+1, -x);  // tree 存的是差分
```

---

## 六、ST 表（Sparse Table）

### 数据结构本质

| 名称 | 含义 |
|------|------|
| `st[i][j]` | 从位置 i 开始，长度为 `2^j` 的区间的最大值（或最小值） |
| `lg[i]` | 预处理 `⌊log₂(i)⌋`，避免浮点 |

### 解决什么问题？

**静态数组的区间最值查询（RMQ）**，O(n log n) 建表，O(1) 查询。

适用于**数组不变、多次查询**的场景。如果数组会变（有修改），用线段树。

### 匹配算法

| 算法 | 解决的问题 | 国赛真题 |
|------|-----------|---------|
| **ST 表 + RMQ** | 多次查询静态区间最大/最小值 | 2022 机房（结合 LCA） |

### 关键性质

1. **只能静态**：建表后不能修改数组
2. **建表**：`st[i][j] = max(st[i][j-1], st[i + (1<<(j-1))][j-1])`
   - 两段 `2^(j-1)` 长度的区间拼成 `2^j`
3. **查询 [l, r]**：令 `k = lg[r - l + 1]`，区间可以重叠但必须全覆盖
   → `max(st[l][k], st[r - (1<<k) + 1][k])`
4. **预处理 lg 数组**省时间：`lg[i] = lg[i/2] + 1`，O(1) 查表
5. 易错：`1<<j-1` 优先级 `-` 高于 `<<`，实际是 `1<<(j-1)`，建议永远加括号

### 代码骨架

```cpp
int st[N][20], lg[N];

// 预处理 lg
lg[1] = 0;
for (int i = 2; i <= n; i++) lg[i] = lg[i/2] + 1;

// 建表
for (int i = 1; i <= n; i++) st[i][0] = a[i];
for (int j = 1; (1 << j) <= n; j++)
    for (int i = 1; i + (1 << j) - 1 <= n; i++)
        st[i][j] = max(st[i][j-1], st[i + (1 << (j-1))][j-1]);

// 查询 [l, r] 最大值
int k = lg[r - l + 1];
int ans = max(st[l][k], st[r - (1 << k) + 1][k]);
```

---

## 七、堆 / 优先队列（Heap / Priority Queue）

### 数据结构本质

| 名称 | 含义 |
|------|------|
| **小根堆** | 堆顶是最小值，`greater<>` |
| **大根堆** | 堆顶是最大值（默认） |
| `pq.push(x)` | 插入，O(log n) |
| `pq.top()` | 取最值，O(1) |
| `pq.pop()` | 弹出最值，O(log n) |

### 匹配算法

| 算法 | 如何用堆 | 国赛真题 |
|------|---------|---------|
| **Dijkstra** | 取出距离最小的未处理节点 | 2022 出差 |
| **哈夫曼编码 / 合并果子** | 每次取最小两个合并再放回 | 经典 |
| **贪心** | 维护动态极值 | 2024 套手镯 |

### 关键性质

1. C++ 里 `priority_queue<int>` 默认是大根堆，**Dijkstra 必须用小根堆**
2. 小根堆声明：`priority_queue<int, vector<int>, greater<int>>`
3. `sizeof(pq)` 是结构体字节数不是元素个数，用 `.size()`
4. 堆的 `top()` 不会自动移除，必须手动 `pop()` 否则死循环

---

## 八、栈 / 队列 / 双端队列（Stack / Queue / Deque）

### 数据结构本质

| 名称 | 操作 | 复杂度 |
|------|------|--------|
| **栈 stack** | push/pop/top，后进先出 | O(1) |
| **队列 queue** | push/pop/front，先进先出 | O(1) |
| **双端队列 deque** | 两端都可 push/pop | O(1) |
| **单调栈** | 栈内元素单调递增/递减（pop 掉破坏单调性的元素） | 均摊 O(n) |
| **单调队列** | 滑动窗口内维护单调性 | 均摊 O(n) |

### 匹配算法

| 算法/数据结构 | 解决的问题 | 典型场景 |
|------|-----------|---------|
| **BFS 队列** | 层序遍历、最短步数 | 2020 扩散、2023 AB 路线 |
| **DFS 栈（递归 / 显式栈）** | 回溯、全排列、路径搜索 | 2019 拼接、2020 玩具蛇 |
| **单调栈** | 找每个元素左边/右边第一个比它大/小的元素 | 直方图最大矩形 |
| **单调队列** | 滑动窗口最大值/最小值 | 2024 套手镯 |

### 关键性质

1. BFS 求最短步数（边权均为 1）是最基础也是最常考的搜索模式
2. 单调栈的核心：while 栈顶不符合单调性就 pop，再压入当前元素
3. BFS 的 `vis` 标记在入队时就设置，不要在出队时设（会重复入队）

---

## 九、字符串（String）

### 数据结构本质

| 名称 | 含义 |
|------|------|
| `next[i]` | KMP 前缀函数：`s[0..i]` 的最长相等前后缀长度（国赛代码中通常用 `pref[i]` 避免与 std::next 冲突） |
| **前缀** | 不含最后一个字符的头部子串 |
| **后缀** | 不含第一个字符的尾部子串 |
| **border** | 同时是前缀又是后缀的子串 |

### 匹配算法

| 算法 | 解决的问题 | 国赛真题 |
|------|-----------|---------|
| **KMP** | 单模式串匹配（找 sub 在 s 中所有出现位置） | 2024 最长回文前后缀 |
| **DP 子序列计数** | 统计子序列出现次数 | 2023 子2023、2020 本质上升序列 |

### 关键性质

1. KMP 核心：构建前缀函数 `pref[i]` = `s[0..i]` 的最长 border 长度
2. 回退：`j = pref[j-1]`（因为已匹配的区间是 `s[0..j-1]`，查这段的相等前后缀）
3. KMP 时间复杂度 O(n+m)，空间 O(m)
4. **B 组国赛 KMP 考频低**（10 年才 1 次），接触板子即可，不强求默写
5. 更常考的是 DP 做子序列相关问题（不依赖 KMP）

---

## 十、线段树（Segment Tree）— B 组偶考

### 数据结构本质

| 名称 | 含义 |
|------|------|
| `tree[p]` | 线段树节点 p 的区间和（或最值） |
| `lazy[p]` | 懒标记：区间修改暂不下传 |
| `push_up(p)` | 用子节点更新父节点 `tree[p] = tree[p*2] + tree[p*2+1]` |
| `push_down(p)` | 下传懒标记给子节点 |

### 解决什么问题？

**区间修改 + 区间查询**，两者都 O(log n)，比树状数组更强（树状数组只能区间修改+单点查询或单点修改+区间查询）。

### 国赛考情

| 国赛真题 | 年份 |
|------|------|
| 第八大奇迹（维护第 8 大值） | 2019 |
| 翻转括号序列（复杂改造） | 2021 |

### 关键性质

1. **B 组偶考，板子长易错，已被列入放弃清单**
2. 能不用就不用——树状数组能解决的绝不上线段树
3. 如果题目确实需要区间修改+区间查询且 n 到 10⁵，才考虑线段树

---

## 附录 A：国赛真题数据结构速查表

按年份排列，快速翻到每道题用了什么数据结构和算法：

| 年份 | 题目 | 数据结构 | 算法 |
|------|------|---------|------|
| 2017 | 发现环 | 并查集 + 邻接表 | 判环 + DFS |
| 2019 | 最优包含 | 字符串 | 编辑距离 DP |
| 2019 | 第八大奇迹 | 树状数组/线段树 | 区间查询 |
| 2020 | 本质上升序列 | 字符串 | DP 子序列 |
| 2020 | 扩散 | 队列 | BFS |
| 2021 | 123 | 前缀和数组 | 前缀和 + 二分 |
| 2021 | 最小权值 | 树 | 树形 DP |
| 2022 | 2022 | 三维数组 | 三维背包 DP |
| 2022 | 出差 | 邻接表 + 堆 | Dijkstra |
| 2022 | 机房 | 树 + ST 表 | LCA 倍增 |
| 2023 | 子2023 | 字符串 | DP 子序列计数 |
| 2023 | 数三角 | 数组 | 计算几何 |
| 2023 | 删边问题 | 图 | Tarjan 割边 |
| 2023 | AB 路线 | 队列 | BFS + 记忆化 |
| 2023 | 抓娃娃 | 差分数组 | 差分 + 前缀和 |
| 2023 | 景区导游 | 树 | LCA 倍增 |
| 2023 | 砍树 | 树 + 差分数组 | 树上差分 + LCA |
| 2024 | 最长回文前后缀 | 字符串 | KMP |

---

## 附录 B：数据结构选择决策树

看到题面后按这个顺序判断：

```
1. 题目涉及"树"（n 个点 n-1 条边连通无环）？
   ├── 问树上两点路径 → LCA
   ├── 问多次给路径加值 → 树上差分 + LCA
   └── 问子树最优解 → 树形 DP

2. 题目涉及"图"？
   ├── 问最短路径，无负权 → Dijkstra
   ├── 问所有点对之间最短路 → Floyd（n≤500）
   ├── 问 DAG 上最长/最短 → 拓扑排序 + DP
   ├── 问最少代价连通所有点 → Kruskal
   └── 问删边/删点 → Tarjan

3. 题目涉及"数组，多次询问/修改"？
   ├── 只询问不修改 → 前缀和（求和）/ ST 表（最值）
   ├── 多次区间修改 → 差分
   ├── 单点修改 + 区间查询 → 树状数组1
   ├── 区间修改 + 单点查询 → 树状数组2（差分）
   └── 区间修改 + 区间查询 → 线段树（慎用）

4. 题目涉及"判断连通/判环/合并集合"？
   └── 并查集

5. 题目涉及"字符串匹配/回文"？
   ├── 单模式匹配 → KMP
   └── 子序列计数 → DP

6. 题目涉及"动态取最值"？
   └── 堆/优先队列

7. 题目涉及"滑动窗口"？
   └── 单调队列 / deque
```

---

## 附录 C：板子默写优先级

| 优先级 | 数据结构/算法 | 理由 |
|--------|-------------|------|
| 🔴 必背 | 前缀和/差分 | 每场必用 |
| 🔴 必背 | 并查集 | 5 行代码，Kruskal 依赖它 |
| 🔴 必背 | Dijkstra 堆优化 | 最短路标配 |
| 🔴 必背 | Floyd | 5 行代码，全源最短路 |
| 🔴 必背 | 拓扑排序 | BFS 变体，简单 |
| 🟡 重点 | LCA 倍增 | 2022/2023 连考 3 次 |
| 🟡 重点 | 树状数组 | 2019 考过，性价比高 |
| 🟡 重点 | ST 表 | RMQ 模板 |
| 🟢 了解 | KMP | 2024 考了一次，非高频 |
| 🟢 了解 | 树上差分 | 2023 考了，依赖 LCA |
| ⚪ 放弃 | 线段树 | B 组偶考，板子太长 |
| ⚪ 放弃 | Tarjan | B 组极少考 |

---

> 最后更新：2026-06-04，基于 2016-2025 年 C/C++ B 组国赛真题统计
